import styled from 'styled-components';

const LetterBox = () => <StyledLetterBox />;

const StyledLetterBox = styled.div`
    border: 2px solid white;
    margin: 0.4rem;
    min-height: 4rem;
    width: 100%;
`;

export default LetterBox;
